AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Revia Phase 1 backend with Cognito, API Gateway REST API, Lambda, DynamoDB, and S3-ready resources.

Globals:
  Function:
    Runtime: nodejs20.x
    Timeout: 10
    MemorySize: 256
    Architectures:
      - x86_64
    Environment:
      Variables:
        AWS_NODEJS_CONNECTION_REUSE_ENABLED: '1'
        COGNITO_USER_POOL_ID: !Ref ReviaUserPool
        COGNITO_CLIENT_ID: !Ref ReviaUserPoolClient
        USERS_TABLE: !Ref UsersTable
        UPLOADS_BUCKET: !Ref UploadsBucket
        COGNITO_AUTO_CONFIRM: 'false'

Resources:
  ReviaApi:
    Type: AWS::Serverless::Api
    Properties:
      Name: revia-phase1-api
      StageName: dev
      Cors:
        AllowMethods: "'OPTIONS,POST,GET,PUT'"
        AllowHeaders: "'Content-Type,Authorization'"
        AllowOrigin: "'*'"

  ReviaUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: revia-phase1-user-pool
      UsernameAttributes:
        - email
      AutoVerifiedAttributes:
        - email
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: false
          RequireUppercase: true
          TemporaryPasswordValidityDays: 7
      Schema:
        - Name: email
          AttributeDataType: String
          Mutable: true
          Required: true

  ReviaUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: revia-phase1-app-client
      UserPoolId: !Ref ReviaUserPool
      GenerateSecret: false
      AccessTokenValidity: 1
      IdTokenValidity: 1
      RefreshTokenValidity: 30
      TokenValidityUnits:
        AccessToken: days
        IdToken: days
        RefreshToken: days
      ExplicitAuthFlows:
        - ALLOW_ADMIN_USER_PASSWORD_AUTH
        - ALLOW_USER_PASSWORD_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH
      PreventUserExistenceErrors: ENABLED

  UsersTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: revia-users-dev
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH

  UploadsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  SignupFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/signup.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref UsersTable
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:SignUp
              Resource: "*"
      Events:
        SignupApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/signup
            Method: post

  VerifySignupFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/verify-signup.handler
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ConfirmSignUp
              Resource: "*"
      Events:
        VerifySignupApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/signup/verify
            Method: post

  ResendSignupOtpFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/resend-signup-otp.handler
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ResendConfirmationCode
              Resource: "*"
      Events:
        ResendSignupOtpApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/signup/resend-otp
            Method: post

  LoginFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/login.handler
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:AdminInitiateAuth
              Resource: "*"
      Events:
        LoginApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/login
            Method: post

  ForgotPasswordRequestFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/forgot-password-request.handler
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ForgotPassword
              Resource: "*"
      Events:
        ForgotPasswordRequestApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/forgot-password/request
            Method: post

  ForgotPasswordConfirmFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/forgot-password-confirm.handler
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ConfirmForgotPassword
              Resource: "*"
      Events:
        ForgotPasswordConfirmApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/forgot-password/confirm
            Method: post

  ChangePasswordFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/change-password.handler
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ChangePassword
              Resource: "*"
      Events:
        ChangePasswordApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/change-password
            Method: post

  MeFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/auth/me.handler
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref UsersTable
      Events:
        MeApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /auth/me
            Method: get

  UpdateProfileFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/users/update-profile.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref UsersTable
      Events:
        UpdateProfileApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /users/me
            Method: put

  DeleteAccountRequestFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/users/delete-account-request.handler
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:GetUserAttributeVerificationCode
              Resource: "*"
      Events:
        DeleteAccountRequestApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /users/delete-account/request
            Method: post

  DeleteAccountConfirmFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: src/handlers/users/delete-account-confirm.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref UsersTable
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:VerifyUserAttribute
                - cognito-idp:DeleteUser
              Resource: "*"
      Events:
        DeleteAccountConfirmApi:
          Type: Api
          Properties:
            RestApiId: !Ref ReviaApi
            Path: /users/delete-account/confirm
            Method: post

Outputs:
  ApiBaseUrl:
    Description: Base URL for the API Gateway stage
    Value: !Sub https://${ReviaApi}.execute-api.${AWS::Region}.amazonaws.com/dev

  UserPoolId:
    Description: Cognito User Pool ID
    Value: !Ref ReviaUserPool

  UserPoolClientId:
    Description: Cognito App Client ID
    Value: !Ref ReviaUserPoolClient

  UsersTableName:
    Description: DynamoDB table for minimal profiles
    Value: !Ref UsersTable

  UploadsBucketName:
    Description: S3 bucket reserved for future uploads
    Value: !Ref UploadsBucket
